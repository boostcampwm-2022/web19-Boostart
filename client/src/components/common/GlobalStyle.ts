import { createGlobalStyle } from 'styled-components';

export const GlobalStyle = createGlobalStyle`
  body {
    background: linear-gradient(#99B1DB 55%, #ffffff 50%);
    height: 100vh;
    place-items: center;
    display:grid;
  }
`;
